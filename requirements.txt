# Core
python-dotenv>=1.0.0
pydantic>=2.0.0

# --- Orchestration framework: LangGraph (chosen) ---
langgraph>=0.2.0
# langchain>=0.3.0
# crewai>=0.60.0
# google-adk>=1.0.0

# --- LLM SDKs ---
openai>=1.40.0             # default provider (LLM_PROVIDER=openai)
# anthropic>=0.34.0       # uncomment (or run: pip install anthropic) if switching to LLM_PROVIDER=anthropic
# google-generativeai>=0.7.0
# groq>=0.9.0

# --- Vector store: Qdrant (chosen) ---
qdrant-client>=1.10.0
fastembed>=0.3.0          # bundled sparse (BM25/SPLADE) + dense models for hybrid search

# --- Data / KB sourcing ---
# datasets>=2.20.0          # Hugging Face datasets

# --- Ticketing (pick ONE when you reach that step) ---
# jira>=3.8.0
# asana>=5.0.0
# notion-client>=2.0.0

# --- Evaluation (bonus) ---
langsmith>=0.1.0           # LangSmith tracing + evaluation SDK
# ragas>=0.1.0
# deepeval>=1.0.0

# --- UI (bonus) ---
gradio>=5.50.0,<6.0  # <6.0 keeps huggingface-hub<1.0 for fastembed compat; 4.x has a starlette templating bug

# --- Testing ---
pytest>=8.0.0
