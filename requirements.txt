# Core
python-dotenv>=1.0.0
pydantic>=2.0.0

# --- Orchestration framework: LangGraph (chosen — see docs/DESIGN_DECISIONS.md) ---
langgraph>=0.2.0
# Other options, if you change your mind later:
# langchain>=0.3.0
# crewai>=0.60.0
# autogen-agentchat>=0.2.0
# google-adk>=1.0.0

# --- LLM SDKs (pick what you need) ---
openai>=1.40.0
# anthropic>=0.34.0
# google-generativeai>=0.7.0
# groq>=0.9.0

# --- Vector store: Qdrant (chosen — see docs/DESIGN_DECISIONS.md) ---
qdrant-client>=1.10.0
fastembed>=0.3.0          # bundled sparse (BM25/SPLADE) + dense models for hybrid search
# Other options, if you change your mind later:
# faiss-cpu>=1.8.0
# chromadb>=0.5.0
# pinecone-client>=5.0.0
# psycopg[binary]>=3.2.0   # for pgvector

# --- Data / KB sourcing ---
datasets>=2.20.0          # Hugging Face datasets
# kagglehub>=0.2.0

# --- Ticketing (pick ONE if going beyond the mock client — still undecided) ---
# jira>=3.8.0
# asana>=5.0.0
# notion-client>=2.0.0

# --- Evaluation (bonus) ---
# ragas>=0.1.0
# deepeval>=1.0.0

# --- UI (bonus) ---
# gradio>=4.40.0

# --- Testing ---
pytest>=8.0.0
